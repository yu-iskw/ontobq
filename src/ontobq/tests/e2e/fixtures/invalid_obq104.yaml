# TEMPLATE — substitute ${ONTOBQ_E2E_PROJECT} ${ONTOBQ_E2E_DATASET} ${ONTOBQ_E2E_GRAPH} ${ONTOBQ_E2E_RUN_ID}
# OBQ104: semantic string mapped to physical INT64 with no CAST.
# Source: isolated e2e_customers_obq104 (schema_obq104.sql). Do not poison e2e_customers.
# Do not apply this variant.
# Expected: diagnostics[].code == "OBQ104"
# Path: spec.entities.Customer.mapping.bigquery.properties.country
apiVersion: ontobq.dev/v1alpha1
kind: Domain

metadata:
  name: commerce_e2e_obq104_${ONTOBQ_E2E_RUN_ID}
  displayName: Commerce E2E OBQ104
  description: Negative — semantic/physical type mismatch.

spec:
  bigquery:
    project: ${ONTOBQ_E2E_PROJECT}
    dataset: ${ONTOBQ_E2E_DATASET}
    graph: ${ONTOBQ_E2E_GRAPH}

  entities:
    Customer:
      key: [id]
      properties:
        id:
          type: string
          nullable: false
        name:
          type: string
        country:
          type: string
      mapping:
        bigquery:
          source: ${ONTOBQ_E2E_PROJECT}.${ONTOBQ_E2E_DATASET}.e2e_customers_obq104_${ONTOBQ_E2E_RUN_ID}
          properties:
            id:
              column: customer_id
            name:
              column: customer_name
            country:
              column: country
