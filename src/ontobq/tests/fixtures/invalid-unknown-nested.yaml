apiVersion: ontobq.dev/v1alpha1
kind: Domain

metadata:
  name: commerce

spec:
  bigquery:
    project: my-project
    dataset: semantic
    graph: commerce_graph

  entities:
    Customer:
      key: [id]
      unexpected: nested
      properties:
        id:
          type: string
      mapping:
        bigquery:
          source: my-project.raw.customers
          properties:
            id:
              column: customer_id
